/**
 * Render component like normal
 * return (
 *      <Route
 *          {...props}
 *          render={matchProps => (
 *              //put rest of components here
 *          )}
 *      />
 * )
 */